"""Repository-level scripts.

This package marker exists so multi-module tooling (``scripts.review_handoff``)
can be imported from the repository root by tests and by ``python -m``.
Single-file scripts here stay directly executable (``python scripts/x.py``).
"""
