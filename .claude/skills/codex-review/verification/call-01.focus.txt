Verification call for a new review procedure. Review the committed diff 78b5e2be1d47f728d1bb078165ee00a8f654d717..HEAD; HEAD is 4254402cbbf9469d808636c6d78418596894c832 and the working tree is clean, so that range is the whole change under review. The change is DOCUMENTATION ONLY: it adds .claude/skills/codex-review/ — a skill describing a review procedure, a review-input template, and six isolated sample files. No executable code, no test, no configuration and no dependency is changed, which is why no repository gate output accompanies this call; there is nothing in the diff that a gate would exercise. Do not treat the absence of gate logs as a claim that any gate passed. Context: this procedure replaces a paused controller program that used to orchestrate reviews. The controller is not in this diff and must not be reviewed; it remains on a separate branch, paused and unaccepted. Approved requirements for THIS change: the procedure must not depend on that controller; it must keep only necessary process documentation, a review input template and verification material; it must not add a scheduler, run-state tracking, history inheritance or authorisation management; and it must state honestly that its limits are a procedure a person follows rather than anything machine-enforced. Approved change scope: .claude/skills/codex-review/ only. Prohibited: bringing any of the paused controller's code into this branch; adding executable machinery of any kind; claiming enforcement the design does not provide. Judge the change on three things, in this order: CORRECTNESS (does the procedure, as written, actually produce a sound review — are there conditions it does not handle, steps in the wrong order, instructions that contradict how the Codex review plugin really behaves, or rules that could be followed literally and still let an unreviewed change through); MAINTAINABILITY (could the next person follow it without the missing controller, and are the samples adequate for checking that it handles a found defect, a failed review and an exhausted budget); and PROJECT FIT (does it follow the conventions in this repository — the gate sequence in CLAUDE.md, the ADR rules, the two gate tiers, the existing skills in .claude/skills/). Pay particular attention to whether anything important was LOST in dropping the controller: a rule the procedure needs but no longer states, or a failure mode that was previously caught by a program and is now only caught if a person remembers. Report only grounded defects: each needs file:line, a concrete scenario in which following the procedure goes wrong, and what you would do about it. This is a report-only review; nothing will be fixed in this pass. If a change you consider NECESSARY falls outside the approved scope above, report it anyway and prefix its title with OUT-OF-SCOPE. End with a single line 'Verdict: approve' or 'Verdict: needs-attention'. Approve only if you found no defect that should block this procedure; if you are unsure, say needs-attention.
